## Task 1: Basic Functions:
    vim functions.sh
        #!/bin/bash
         #function to greet user
         #function to add two numbers
         #function call

         greet() {
           echo "Hello, $1
           }
           add () {
            sum=(( $1 + $2))
             echo "sum = $sum"
             }
             greet Geet
              add 10 20
     OUTPUT:
     
       
